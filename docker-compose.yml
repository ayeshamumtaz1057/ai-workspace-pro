services:
  app:
    build: .
    ports:
      - "8501:8501"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GEMINI_MODEL=gemini-3.6-flash
    volumes:
      - ./data:/app/data       # keeps the SQLite database across restarts
    restart: unless-stopped
